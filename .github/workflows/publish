# Uncomment this when ready to publish
# on:
#   release:
#     types: [created]

# jobs:
#   release:
#     if: github.event.release.draft != true && github.event.release.prerelease != true
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v4
# http://127.0.0.1:4002/projects/wasp/      - uses: atopile/publish-package@main
